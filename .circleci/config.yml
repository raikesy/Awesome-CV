version: 2
jobs:
  build:
    docker:
      - image: thomasweise/texlive
    steps:
      - checkout
      - run: make
      - deploy: 
          command: mkdir output; mv examples/*.pdf $_
      - store_artifacts:
          path: output