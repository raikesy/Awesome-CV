version: 2
jobs:
  build:
    docker:
      - image: "raabf/latex-versions:texlive2018"
    steps:
      - checkout
      - run: 
          name: Compile PDFs
          working_dir: src
          command: latexmk -pdfxe
      - deploy:
          name: Move PDFs to output folder
          command: mkdir out; mv src/*.tex $_ 
      - store_artifacts:
          path: out